# Implementation coverage

What Graphite does today, how each part is verified, and what is still missing. `OBJECTIVE.md` describes the complete product; this file tracks progress toward it. Update it with every change that adds, removes, or re-verifies behavior.

Last updated: 2026-09-25.

## How to run the checks

The repository sits in an iCloud-synced Desktop folder. iCloud adds extended attributes that make code signing of test bundles fail, so build outside it:

```bash
swift test --scratch-path /tmp/graphite-build
```

iPad-only tests (PencilKit) run on a simulator through the app project. The Graphite scheme's `GraphiteIntegrationTests` target compiles and runs six files of `Tests/GraphiteAppleTests` (`InteroperabilityTests`, `PencilDrawingTests`, `PDFAnnotationTests`, `PDFPencilInkTests`, `VaultLocatorTests`, `VectorDrawingTests`) plus `AppIconBundleTests`, so copying the package outside the project (the `Graphite-Package` scheme) is no longer needed for them:

```bash
xcodebuild -project Graphite.xcodeproj -scheme Graphite -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5)' test
```

The other files of `Tests/GraphiteAppleTests` are not in that target, so their UIKit-only tests (`testSecondEraserCutInsideAStrokeIsConvertedAgain` and `testDeferredRecordIsWrittenOnceByTheTracker` in `ApplePdfFixTests`) run nowhere yet.

Latest results (2026-09-25, after the bug-fix merge): `swift test` on macOS ran 1,117 tests: 1,116 passed and one was skipped (`UiBasesReloadMeasurementTests.testMeasureRepeatedReloadOfLargeBase`, which measures only when `GRAPHITE_MEASURE_BASES` is set). By target: 568 core, 127 index, 123 Apple, 299 UI. The app also builds for iOS (`xcodebuild -project Graphite.xcodeproj -scheme Graphite -destination 'generic/platform=iOS Simulator' build`). No test ran on an iPad simulator or a device after the merge: the iPad-only tests above were last run before the search and editing changes of 2026-09-23 (evening) and need a fresh run, and the iOS-only code the merge changed is verified only by that build (see "Implemented but not yet verified").

## The test vault

`Tests/test_vault/` (not committed; see `.gitignore`) is a copy of a real course vault plus edge cases for testing by hand. Every edge-case note starts with a "What to check" callout saying what correct behavior looks like, compared with Obsidian.

- `Restaurants/`: a real Obsidian base with cards over remote cover images, a map with Lucide pin icons, and views embedded by name in `Restaurants.md`. The Google-hosted cover URLs answer 403 outside a browser session, so they show as broken images in Graphite (and in any app that is not signed in).
- `Edge cases/Markdown/`: headings and text (RTL, CJK, emoji, long words), lists and tasks (custom statuses, deep nesting), links (aliases, headings, block references, ambiguous and unresolved, Unicode names), embeds (notes, sections, blocks, loops, images with sizes, media, PDFs, bases), every callout type, math (environments, matrices, invalid LaTeX, dollar amounts), tables, 31 property edge cases (invalid YAML, CRLF, byte order mark, duplicate keys), Colors, code, footnotes, comments, HTML, a mixed lecture note, and a 3,000-line note.
- `Edge cases/Bases/`: 93 notes (books, people, courses, assignments, places in six cities, odd records) and 18 bases, including broken ones. `00 Bases overview.md` gives the expected counts and summary values per base.
- `Edge cases/Media/`: images (EXIF rotation, transparency, GIF, HEIC, WebP, CMYK, 16-bit, 8000×6000, fake PNG), audio and video (including corrupt, portrait and HEVC), PDFs (outline, annotations from another app, rotated and mixed page sizes, password `graphite`, corrupt, empty, page labels, scans, 100 pages). `Media gallery.md` embeds them all.
- `Edge cases/Files/`: encodings (CRLF, CR, BOM, UTF-16, Latin-1, binary), 0-byte and 2 MB notes, odd names, hidden files, non-note files, a folder named like a note, twelve levels of folders, 250 linked notes, and symbolic links inside and outside the vault.

## Implemented and covered by automated tests

| Area | Behavior | Evidence |
| --- | --- | --- |
| Vault paths | Relative paths, `..` escapes and symbolic links outside the vault rejected; always in Unicode's composed form, whether given decomposed (as file URLs give them) or decoded from a stored layout, so one file is one index row whichever form refreshed it; backslashes in existing names, a combining mark after `/`, and chained symbolic links through a dangling target | `VaultTests`, `IncrementalIndexTests`, `CoreFilesSettingsFixTests` |
| Vault list | Picking a listed folder again updates its entry; a moved vault is followed by its identifier; alphabetical order and the most recent restored at launch; the last open document stored; removal; names for new vaults; locations worded as in Files (On My iPad › Graphite for Graphite's own folder, iCloud Drive › Obsidian; file-provider storage as Files › folders, since On My iPad and third-party providers share one path layout) | `VaultListTests` |
| Vault folders | A new vault is an empty folder, found again through the folder it was created in; an existing item is never reused; a picked folder is followed when renamed and reported when deleted; a folder inside Graphite's own folder is remembered by its path there (iPad simulator); Graphite's own Documents folder, or a vault inside it, picked as a vault is remembered by its path; a vault folder renamed or deleted by another app is reported, a moved one is opened again at its new place, and a deleted one asks to be opened again | `VaultLocatorTests`, `AppleRecordingAccessFolderTests`, `UiWorkspaceAreaHandoffTests` |
| Safe writes | Revision-checked atomic replace; same-size external edits and deletions are conflicts; exact bytes preserved; staged outside the vault folder, leaving nothing behind; the system trash asks other presenters to let go first | `VaultTests`, `CoreFilesSettingsFixTests` |
| File management | Renaming a note rewrites every link to it in its own style (bare names, headings with aliases, embeds, Markdown links, vault paths, links in properties, links in tables with `\|`), leaving code alone; moving to another folder recomputes relative links, including the moved note's own; renaming a folder follows paths into it; a name that becomes ambiguous gets a path; a note changed after planning is left alone and reported; moves never replace another item or put a folder inside itself; copies are named "Name 1"; `.trash` and permanent deletion; names Obsidian refuses are refused; Markdown link destinations read with CommonMark's rules (balanced parentheses, titles, code spans and images in labels), a link inside another link rewritten as one edit, reference-style links updated through their definition, a quoted property link escaped for its quotes, letters beyond ASCII kept in the form they were written, bare names and aliases that still find their target kept, Markdown links in shortest and absolute format keeping that format; linking notes that cannot be read, are too large or not UTF-8, or whose links cannot be located are left untouched and listed with their reason; a leading byte order mark kept; a folder's files listed from the disk as well as the index; links spelled with other capitals rewritten on case-insensitive volumes; a move without link updates still reports every moved file; nothing moves until the index has read the vault | `FileOperationTests`, `CoreLinksRewriterTests`, `CoreLinksFileOperationTests`, `IndexFileOperationsFixTests`, `UiWorkspaceAreaHandoffTests` |
| Pasted web pages | HTML converted to Markdown: headings, emphasis, links, images (not inline data), nested lists, quotes, code blocks, inline code, tables, line breaks; scripts, styles and comments dropped; entities decoded; broken markup and a bare `<` tolerated; paragraphs and headings inside list items, blocks inside table cells, escaped pipes in cells and link destinations, code with backticks, markup inside `pre` kept as code, quote and list prefixes on code, tables and rules, the Google Docs wrapper, empty and nested emphasis, script and style text skipped, an unclosed `head`, `ol start` and `li value`, text after a nested list, script links in any case or spelling (including entity and backslash tricks), escaped alt text and destinations, Latin-1 and Greek entities, no-break spaces kept between words, a byte order mark, the first line break in `pre`, linear time on unclosed tags, bounded nesting | `HTMLToMarkdownTests`, `CoreHtmlColorsHTMLToMarkdownTests` |
| Block links and embeds | `![[Note#^id]]` shows only that block; a heading subpath shows its section; `^id` markers hidden when reading and concealed in Live Preview | `BlockEmbedTests`, `NotePreviewDocumentTests` |
| Link and tag suggestions | What is being typed at the cursor: `[[` with a note, `#heading` or `#^block` part, embeds, a closing `]]` already there, nothing inside code or past the alias; choosing inside an existing link replaces the rest of it and keeps an alias already written; `#tags` after a space, including digits so far, emoji tags and tags after punctuation; no suggestions in fences inside block quotes, code spans across lines, HTML comments, after escaped brackets, or on an empty last line; link text follows the vault's link format; blocks (paragraphs, list items with their continuation lines, whole lists, tables and quotes with their lazy continuation lines, and code; not setext headings, thematic breaks, code inside list items, or indented code), each with its `^id`, including Obsidian's own-line form after a blank line; a new `^id` goes after a blank line for tables, quotes and lists, in the note's line endings; tags counted per note, whatever their capitals | `LinkCompletionTests`, `TagSuggestionTests`, `CoreLinksCompletionTests`, `CoreFrontmatterBlockTests`, `IndexSearchFixTests`, `UiEditorAreaHandoffTests` |
| Footnotes | References, definitions with indented continuation lines, and inline footnotes, outside code; numbered by first reference with labels ignoring case, repeats keeping their number, unreferenced definitions last; undefined references left as written; the Footnotes view's list and locations | `FootnoteTests` |
| Folding | Headings fold to the next heading of the same or higher level, keeping blank lines before it visible; list items fold their indented lines; headings with nothing under them, in code and in frontmatter do not fold; keys survive edits elsewhere and count repeats; nested folds; which folds hide a location; Return at a folded line starts a sibling item after the section (tasks stay tasks, numbers continue) | `NoteFoldingTests` |
| Mentions | Unlinked mentions as whole words, ignoring case and accents, outside links, embeds, code, math, comments, tags, web addresses, HTML tags and frontmatter, preferring the longest of overlapping names; links that could name a note (by name or alias, embeds and web links left out); a mention linked as `[[Note]]` or `[[Note\|words]]` or a Markdown link; excerpts mark the range | `MentionTests` |
| Templates and daily notes | Moment.js formats (tokens, ordinals, bracketed text, ISO weeks, long formats, time zones) and reading dates back from names, rejecting impossible days; `{{title}}`, `{{date}}`, `{{time}}` with formats and offsets; `templates.json` and `daily-notes.json` read with Obsidian's defaults and written keeping unknown keys; daily note paths with folders from the format; a template's body goes in at the cursor, never inside frontmatter, and its properties are merged (lists gain items, existing values stay, empty ones are filled) | `MomentDateFormatTests`, `TemplateTests` |
| PDF links and quotes | Page links in Wikilink and Markdown form, labels and paths escaped; quotes join a PDF's broken lines and hyphenated words, keep hyphens before digits and capitals, and keep paragraphs; a quote inserted into a note is kept apart from the lines around it | `PDFCitationTests` |
| File recovery | A copy at most once per interval, none for repeated or blank text, and one taken anyway after a copy dated in the future; newest first; old copies pruned and notes left without any removed; at most 200 per note; copies follow a renamed note or folder; the editor reports its saved text before a save or a reload replaces it, and nothing when nothing changes | `FileRecoveryTests`, `MarkdownSessionRecoveryTests` |
| Tags and Properties views | Every level of nested tags counted once per note (tags in text and in properties together, capitals merged); tags nested into a tree, a missing level added, each level sorted by name or frequency with ties broken by name; property names counted ignoring capitals with a few values each; types from `types.json` (ignoring capitals), then the name (`tags`, `aliases`), then the first non-empty value; renaming a property changes only its name where it is written (quoted names, CRLF, comments and lists kept byte for byte), matches names ignoring capitals, quotes a new name YAML would misread, keeps the cursor on the same text, and refuses invalid YAML or a name the note already has; `types.json` assignments set and moved keeping other keys, left untouched when nothing changes, never overwritten when it is not a JSON object | `TagsAndPropertiesTests`, `VaultTagsAndPropertiesTests` |
| Bookmarks | Obsidian's `bookmarks.json` read with every kind (files with a heading or block, folders, searches, groups, graphs, web pages) and written keeping unknown kinds, keys and whole-number `ctime`s; titles as Obsidian shows them; add, rename (an empty name restores the default) and remove, inside groups too; renamed files and folders followed, headings kept; files not in Obsidian's format refused and never overwritten; changes applied to the file as it is now, keeping one made elsewhere; no file created for nothing, an unchanged list leaves it as it was | `BookmarkTests` |
| Index location | Named after the vault's identifier, so it survives the app's folder moving; an index named after the old path is moved with its journal; indexes unused for 30 days deleted; a removed vault's index deleted; a vault's identifier known before it is recorded; hidden files such as `.obsidian` settings never indexed, even when a change to one is reported | `IncrementalIndexTests`, `VaultListTests`, `VaultTests` |
| Graph view | The graph holds files, links between them (text and properties, embeds included, a link written twice once), links without a file merged ignoring capitals, and tags; Obsidian's filters (tags, attachments, existing files only, orphans, a path search ignoring capitals and accents); the local graph to a depth, following incoming or outgoing links, with or without links between neighbors; ambiguous and web links left out; the in-memory resolver agrees with the index for 174 cases (case, accents, aliases, partial paths, extensions, relative Markdown links, headings); the layout keeps linked notes closer than others, is the same every run, stays finite from nodes all in one place, keeps an earlier layout's shape and starts new nodes beside a neighbor, holds a dragged node, and its quadtree matches the exact forces | `LinkGraphTests`, `VaultGraphTests`, `GraphLinkResolverTests` |
| Deep links | `graphite://` and `obsidian://` read with Obsidian's actions and parameters, ignoring capitals, flags without a value on, `overwrite` before `append`, `obsidian:open` and `vault/Name/file` forms; other links refused; links written as Obsidian writes them (spaces as `%20`, notes without `.md`) read back; appended text on a line of its own | `VaultURITests` |
| Recordings after a crash | A recording written with the recorder's own settings (IMA4 in CAF) and cut off before it is closed converts to an M4A of nearly its full length; the original stays until the M4A is in place; unfinished recordings are found oldest first with where they belong, leaving out the one being made, files with no audio (a header alone) and other files; a file without audio is refused and kept | `RecordingRecoveryTests` |
| Links at the cursor | The Wikilink (target without alias) or Markdown link around a character, web links apart, embeds excluded, the position right after a link counted only for the keyboard | `LinkLocatorTests` |
| Navigation | Fuzzy matching prefers word starts and runs, ignores accents, and reports what matched; back and forward drop what was ahead after a new visit, follow renames and forget deleted files; recent files stay newest first without repeats; the quick switcher finds names, paths and aliases; sharp s and ligatures fold into several letters, no match lost after skipping to a word start, and the ASCII path agrees with the general path; the quick switcher, through the index, finds names with other capitals and accents (`uber` finds `Über.md`, and `ÉLAN` finds `Élan vital.md`) | `NavigationTests`, `QuickSwitcherTests`, `CoreLinksFuzzyMatcherTests`, `IndexSearchFixTests` |
| Tabs and split | Opening in the current tab, a new tab, or the other side (created when needed, its empty tab reused); pinned tabs keep their file; closing activates the right neighbor then the left, closes an emptied side, and leaves the last side one empty tab; Close Other Tabs keeps pinned tabs; reopening skips files open again; moving tabs between sides and reordering; next, previous and ⌘1–⌘9; per-tab history; showing a tab on the other side without moving focus; renames and deletions reach every tab, history and the closed list; saved layouts restore without missing or repeated files; the divider stays between 25% and 75% | `TabLayoutTests` |
| Vault settings for files | `trashOption`, `alwaysUpdateLinks`, `promptDelete`, `fileSortOrder`, `newFileLocation` and `newFileFolderPath` read, and written only when changed in Graphite; values changed in Obsidian meanwhile, values Graphite reads differently, duplicate keys and Obsidian's layout are preserved; an empty `app.json` reads as Obsidian's defaults; sort orders keep folders first | `SettingsAndAttachmentTests`, `CoreFilesSettingsFixTests`, `UiWorkspaceAreaHandoffTests` |
| Obsidian settings | `.obsidian/app.json` keys read and written with Obsidian's values; unknown keys preserved; malformed files never overwritten; folder created when missing | `SettingsAndAttachmentTests` |
| Attachment placement | Vault folder, specified folder, same folder, subfolder; escape rejected | `SettingsAndAttachmentTests` |
| Embeds | Shortest, relative, absolute; Wikilinks or Markdown links; names with `#`, `|`, `[`, `]`, `^` fall back to Markdown links; image sizes after the last `|` (`![Caption|150](…)`, `![[x.png|Caption|300x100]]`), fitted inside width × height without stretching | `SettingsAndAttachmentTests` |
| Embedded image decoding | Images and drawings on their own line in reading view and Live Preview decoded once, off the main thread, for the width they are shown at (the readable column or Obsidian's width size, times the display scale); a tall image keeps that width, the longest side stays bounded, EXIF-turned photos and PDF pages are sized as shown; each width cached until the file changes; decoded pixels drawn in the first render, with no PNG step | `ImagePreviewSizingTests`, `UiEmbeddedImageDecodingTests`, `UiReadingViewTests` |
| Block insertion | Embeds go on their own line after frontmatter, keep blank lines, never create a setext heading | `BlockInsertionTests` |
| Markdown semantics | Links, embeds, headings, tags, aliases; frontmatter tags with numbers or written as comma-separated text; code excluded; LF, CRLF and empty frontmatter; Obsidian's tag rules (`#2026-exam` is a tag, `#1984` is not, emoji and nesting allowed, `[[#Heading]]` is not a tag); code spans and fences found for link rewriting | `VaultTests`, `MarkdownCodeRangeTests` |
| Editing commands | Heading 1–6 applied to the whole line (or every selected line), replacing other heading marks, toggled off when repeated; CRLF kept; the cursor stays on its word | `MarkdownEditingTests` |
| Typing helpers | Return continues bullets, numbers (the next number, `.` or `)`), tasks (an open checkbox), and quotes, and renumbers the numbered items that follow while they count up by one; an empty item ends the list or moves out a level, and an empty nested numbered item that moves out continues the parent's numbering; Return inside fenced code or frontmatter is a plain line break; indent and outdent with tabs or spaces inside quotes, with `tabSize` as the width of space indentation in a tab vault; toggle checkbox, bulleted and numbered lists; move lines up and down; Return and moving lines keep CRLF and CR line endings; bold, italic and the like wrap the selection or the word and unwrap it again, and italic inside bold adds italic; brackets and quotes pair, step over their closing half, and are deleted together; quotes after a letter and a third backtick are typed alone; of Obsidian's typing settings, `autoPairBrackets` is written alone when it changes and `tabSize` is read (at most 8) and kept as written | `SmartEditingTests`, `SettingsAndAttachmentTests`, `CoreEditingFixTests`, `CoreFilesSettingsFixTests`, `UiEditorAreaHandoffTests` |
| Search syntax | Words, `"phrases"` (also with the iPad's curly quotes), `-` exclusion, `OR`, parentheses, `file:`, `path:`, `content:`, `tag:`, `line:`, `section:`, `task-todo:`, `task-done:`, `match-case:`, `[property]`, `[property:value]`, `[property:<5]`, `/regular expressions/`; properties searchable as text; words folded exactly as the full-text index folds them (`Straße`, `ﬁle` and `Crème` found as typed), with the folding checked against SQLite on every assigned character; words inside Chinese, Japanese and Thai text, symbols and emoji found as text, also with `line:`; `md` not found in every note's extension; `file:` and `path:` ignoring case in every script; an invalid `/pattern/` reported, and a runaway one stopped with an error, also in `line:`; nesting past 32 levels read flat; matching lines with the words marked and their position in the file; pages that continue after the last file checked, in every sort order, without gaps or repeats when files change; a first page keeps checking past 5,000 candidates until it finds a match; a long search leaves the index to other lookups; cancellation | `SearchQueryParserTests`, `SearchSyntaxTests`, `IndexSearchFixTests`, `CoreAreaIndexHandoffTests`, `CoreSearchQueryFixTests`, `CoreSearchQueryIndexSearchTests`, `CoreSearchQueryUnicode61AgreementTests` |
| Editor styling | Headings, emphasis, code, math, links, embeds, tags, callouts, lists, tasks, frontmatter, fences; CRLF; which markup Live Preview hides; `[[#Heading]]` reads as "Heading", `[[Note#Heading]]` marks its `#` separator | `MarkdownStyleScannerTests` |
| Reading view text | Obsidian single-line breaks (unless strict line breaks is on) | `PreviewTextTests` |
| Reading view blocks | Headings with anchors, callouts (folded, unfolded, nested body), display math blocks, whole-line embeds, base code blocks; outline skips code, including four-backtick fences; a heading's section for `![[Note#Heading]]`; highlights, colors and comments prepared for rendering; `$$…$$` inside a table cell made inline; multi-line display math joined and its LaTeX protected from Markdown (comments, quotes, tables, code left alone, dollar amounts not math); MathJax environments rewritten; task checkboxes marked outside code; `%%` comments removed before blocks are split, and `%%` in code (also in fences inside quotes and callouts) kept; `$$` paired by the same rule everywhere; callouts nested at most 16 levels; any task status a checkbox; private-use marker characters in a note shown as U+FFFD; `Note#Parent#Child` sections; alignment environments drawn as `aligned` and formulas the typesetter accepts after rewriting | `NotePreviewDocumentTests`, `CorePreviewDisplayMathTests`, `CorePreviewCommentTests`, `CorePreviewHeadingAndSectionTests`, `CorePreviewBlockTests`, `CorePreviewInlineMarkupTests`, `CorePreviewLaTeXTests`, `CorePreviewTypesetterCompatibilityTests` |
| Reading view links and rendering | Markdown links followed from the note's folder (`../`, `#heading`, `#page`), link destinations found after the label and before a title, links in embedded notes followed from that note by exact relative path, file links under a `/private` root, blocks starting with a rule keep their Wikilinks, link targets and image names with unbalanced parentheses or trailing backslashes, task checkboxes in linear time, markers before combining marks, private-use characters in notes kept, oversized embedded notes shown as links, unresolved inline embeds rebuilt once indexed, one scroll target per heading name, media players kept per note once playing, build keys, frontmatter-only parsing, thumbnail cache reuse and inline image decoding | `UiReadingViewTests` |
| Editor and reading view follow-ups | The Outline jumps to the chosen one of several headings that read the same, in the editor and in reading view (a heading in a callout is not counted, as the outline does not list it); every queued insertion applied in one editor update (Mac editor); outdent uses the vault's `tabSize` for space-indented lines in a tab vault; a chosen link suggestion keeps an alias already written; editing a setext underline restyles the title above it; reading view reads properties with `.obsidian/types.json` types and a property edit keeps declared values as written; Live Preview blocks rebuilt when the types change; Live Preview links and images with an unbalanced parenthesis or a trailing backslash stay whole | `UiEditorAreaHandoffTests`, `UiEditorHeadingJumpTests` |
| Live Preview blocks | Frontmatter, tables (GFM delimiter row required), math blocks (a formula opened after text stays in its paragraph and never covers a later heading), whole-line embeds (web images and image titles excluded from the target), base code blocks, horizontal rules (not setext underlines), callouts with their quoted body | `LivePreviewBlockTests`, `CoreAreaHandoffTests` |
| Properties | YAML 1.2 core types, declared types from `.obsidian/types.json`, lists, dates, links; Obsidian-style serialization; the body and CRLF line endings kept exactly when properties change; untouched numbers keep their spelling (`007`, `1.10`) even when a flow mapping is rewritten; nested aliases refused quickly; untouched properties kept byte for byte (comments, anchors, `007`, large IDs, nested lists, complex keys); comment-only frontmatter; control characters and YAML line breaks quoted | `NotePropertiesTests`, `CoreAreaHandoffTests`, `CoreFrontmatterUntouchedPropertiesTests`, `CoreFrontmatterValueWritingTests` |
| Colors syntax | `~={#hex}text=~` and palette names, nesting (innermost wins), ends at blank lines and with its list item, heading or table cell, ignored in code spans of any length, longer fences and inline math, `=~={` not a closer; list items and headings inside quotes, fences interrupting a paragraph, tables without outer pipes, setext headings, `$` in code spans not paired as math, a backtick line with a backtick in its info string not a fence, code in quotes and indented code not colored, palette names that look like hex, `$$…$$` followed by text, linear time on dense notes | `TextColorMarkupTests`, `CoreHtmlColorsTextColorMarkupTests` |
| Bases language | `.base` parsing (filters as strings or nested and/or/not, formulas, properties, summaries, table/cards/list/map views); expressions, functions, dates and durations, `this`; errors per cell, including a regular expression that runs past one second; links resolved once per folder; date arithmetic as in Obsidian (months overflow like JavaScript's `setMonth`, days and hours move the local wall clock across daylight-saving changes, and a repeated local time takes its first occurrence); date text in moment's English locale whatever the device's; `format()` with the long formats (`L`, `LL`, `l`, …) and five-digit years; JavaScript's rules for `replace`, `split` and regular expressions; deep formula chains fail instead of exhausting the stack; huge, infinite and NaN numbers never trap | `BaseDefinitionTests`, `BaseEvaluatorTests`, `BaseColorParsingTests`, `CoreAreaHandoffTests`, `CoreBasesDatesFixTests`, `CoreBasesEvaluatorFixTests`, `CoreBasesValuesFixTests` |
| Bases queries | Filtering, sorting (dates, numbers, date-like text, and mixed text, links and numbers in one consistent order), grouping (numeric text in numeric order, links grouped by the note they point to), limits, the default and custom summaries; map `zoom`/`height` read like `defaultZoom`/`mapHeight`; views keep their position in the file's list when entries before them are not views; a repeated column shown once | `BaseQueryTests`, `CoreBasesDefinitionQueryEngineTests`, `CoreBasesDefinitionQueryFixTests` |
| Bases editing | `.base` edits keep unknown keys and values exactly (including emoji) and CRLF line endings; an unchanged base is written back byte for byte, and an edit changes only the edited lines (comments, explicit tags and each line's ending kept, lists indented like the file's); filters Graphite cannot fully read, and `views` that is not a list, are never overwritten; merge keys apply and null values are absent keys; property edits refuse changes that would alter another property, new values for a property holding a mapping, and values that would not read back as written; the cell editor is given the property as written; a byte order mark kept; an open tab adopts a property set from a base; `[[Books.base#Gallery]]` and an embed's Open Base open that view; `BaseDocumentModel` refuses an edit prepared on a view that moved and never replaces a newer definition with an older reload. Saves are revision-checked in the code, but no test changes the file between reading and saving | `BaseDefinitionEditorTests`, `BasePropertyEditingTests`, `CoreAreaHandoffTests`, `CoreFrontmatterBaseEditingTests`, `CoreBasesDefinitionQueryFixTests`, `UiBasesDocumentModelTests`, `UiWorkspaceAreaHandoffTests` |
| Bases index | Frontmatter and property links stored per note, creation dates, SQL prefiltering by folder, tag, extension, property, property equality (`status == "reading"`) and `hasLink`, checked to keep every row the evaluator keeps (padded link targets, aliases, partial paths, links written in another case outside ASCII, stored names such as `straße` that Foundation folds into ASCII); the 5,000-record cap reported; a sample vault (books, places with coordinates, tasks, courses); the `hasLink` prefilter keeps relative, partial, rooted and alias links and resolves a written target from every folder; an empty extension does not narrow; database errors in evaluator lookups are recorded and reported instead of empty answers | `BaseIndexTests`, `BaseSampleVaultTests`, `CoreAreaIndexHandoffTests`, `IndexFileOperationsFixTests`, `UiWorkspaceAreaHandoffTests` |
| Index | Full-text, tag, alias search; file-name matches listed first without duplicates, `%` and `_` literal; ambiguity; backlinks including relative Wikilinks, partial, vault-rooted and alias links, frontmatter links, Markdown links in Obsidian's shortest, absolute and relative formats (beside the note, then from the vault root, then by name; aliases name only Wikilinks) and case differences, including letters beyond ASCII, with the exact spelling preferred; lookups and the prune use indexes rather than table scans; existing rows migrated to folded keys; rename plans for partial-path links; links to dotted names, names stored in decomposed Unicode, and partial paths; notes saved during a scan are neither pruned nor overwritten; backslash names; unindexable notes not read again; byte-limited batches; a failed write costs only its file; per-file refresh, skipping hidden and symbolically linked paths, removing a deleted folder's files, and continuing past unreadable ones; scans across chunks and batches read only changed files; an unreadable note in a coordinated batch costs only itself; a cancelled link query does not run; pruning continues past unreadable notes; stored paths stay correct when the file enumerator reports the vault under another path prefix (every scan of a temporary folder meets this on macOS, which enumerates `/var/…` as `/private/var/…`; no test checks it by name) | `IndexTests`, `IncrementalIndexTests`, `BaseIndexTests`, `CoreAreaIndexHandoffTests`, `IndexVaultIndexLinkTests`, `IndexVaultIndexRenameTests`, `IndexVaultIndexScanTests`, `IndexVaultIndexScanBatchTests` |
| Byte order marks | A leading UTF-8 byte order mark kept when a note is saved, when its links are rewritten after a rename, when a block `^id` is added to it, and when a property is set from a base, and when a `.base` file is edited; no mark added to a note without one | `UiMarkdownPaneFixTests`, `IndexFileOperationsFixTests`, `UiBasesDocumentModelTests` |
| PNG drawings | Metadata round trip; corrupt metadata falls back to a readable image; ImageIO decodes output; the digest covers every chunk but the record and text and time chunks, so colour, orientation or animation edits make strokes stale, while drawings saved with the older pixel-only digest stay editable; malformed and sliced files; deeply nested metadata discarded on a small stack; tall PNGs encoded in bands that start on whole pixel rows, and one taller than 32,768 rows keeps its background and ink; a file that decodes to something else is refused | `InteroperabilityTests`, `PencilDrawingTests`, `CoreDrawingDataFixTests`, `AppleDrawingFixTests` |
| PDF drawings | One page, opens in PDFKit and CoreGraphics; ink is page content; metadata round trip; annotations added elsewhere make strokes non-editable; rotated, cropped or restyled drawings are no longer editable, while resource rewrites that show the same stay editable; compressed bombs are not inflated past the budget; multi-page files are not decoded | `VectorDrawingTests`, `PencilDrawingTests`, `AppleDrawingFixTests` |
| SVG drawings | Well-formed XML (Foundation `XMLParser`); metadata round trip; edits elsewhere make strokes non-editable; locale-independent numbers | `VectorDrawingTests`, `PencilDrawingTests` |
| Vector ink | Outline area, dots, round joins at hairpin turns, erased gaps kept as separate pieces; coverage within 35% of PencilKit's own rendering; ink drawn in sRGB in every format, wide-gamut components clamped; the eraser mask keeps its gap open in the stroke's own space | `VectorDrawingTests`, `PencilDrawingTests` (simulator), `AppleDrawingFixTests` |
| Drawing export | Crop to ink at full canvas width; 2× PNG; oversized canvases rejected before allocation with the original file kept | `DrawingGeometryTests`, `PencilDrawingTests` |
| PDF notebooks | Paper templates valid; page insert, duplicate, delete, move, rotate, export survive reopening; standard ink persists; erased gaps stay separate in `/InkList`; dotted paper shares one dot cell; inserted paper matches the page size, including scan-sized pages | `InteroperabilityTests`, `AppleDrawingFixTests`, `UiWorkspaceAreaHandoffTests` |
| PDF annotation | Ink saved as standard `/Ink` whose appearance stream is the variable-width outline (checked with CoreGraphics); per-stroke incremental updates and merged edits; ink changed by another app; highlight, underline and strikeout markup added, recolored and removed, including unnamed markup from other apps; bookmarks written; the saved file becomes the next baseline; conflicts; export; annotations and Graphite keys survive page changes; the warning when page changes would drop document settings; `#page=` and `#height=` embed options; outline entries and links follow moved pages and are removed with deleted or unexported pages (an entry keeps its children); another application's named markup found again after a page change and restored with its note, author and exact color; signed PDFs and a declared PDF version reported before page changes | `PDFAnnotationTests`, `ApplePdfFixTests`, `UiWorkspaceAreaHandoffTests` |
| PDF sessions and views | Later saves replay only newer edits; overlapping saves keep every edit; an external change under unsaved edits is a conflict, and Graphite's own saves are not; a failed save keeps the edits and is reported until a save succeeds, and a failed autosave is retried; autosave waits longer after slow saves and after failures; permission-restricted, encrypted and signed PDFs are shown without changes, a signed one until the user accepts invalidating the signature; baseline copies of ended processes removed; form fields on shown pages read-only and the file untouched; thumbnails drawn off the main thread from a mapped copy, cached per size, and released with their PDF; a second embed that starts annotating saves the first and reads the file again; a session whose closing save failed is kept for the next embed of its PDF; a tab's PDF reports its saves to the index | `UiPdfSessionFixTests`, `UiPdfViewsTests`, `TestsExistingAppleServiceTests`, `UiWorkspaceAreaHandoffTests` |
| Recording controller | A failed recording blocks a new one until it is saved again or discarded; saving again can target a new destination after the folder was renamed; a taken name is reported; a recording is saved even when its recovery copy cannot be deleted; Stop finishes even when the recorder never reports finishing; an abandoned recorder leaves the audio to save again | `AppleRecordingAccessRecordingTests`, `UiWorkspaceAreaHandoffTests` |
| Drawing editor drafts | New drawings take the note width limit; a draft reopens with its strokes until the editor closes; a draft removed right after writing leaves nothing; an existing drawing's draft keeps its revision for conflict checks; a draft that failed to encode is not offered | `UiRootSettingsTests` |
| PDF Pencil ink | Real PencilKit strokes, including width variation surviving a save | `PDFPencilInkTests` (iPad simulator) |

## App icon presets (2026-09-23)

- Implemented: five bundled Icon Composer documents (blue primary, red, black, charcoal, blue graphite) and an iPhone/iPad Appearance → App icon picker with previews, selected state, in-progress disabling, error reporting, and selection refreshed from `UIApplication`. This is separate from the existing custom accent-color picker. macOS uses the blue primary icon without the alternate-icon picker.
- Verified: Debug application builds for the iPad Pro 13-inch (M5), iOS 27 simulator, and macOS. `AppIconBundleTests` runs in the application host and checks primary/alternate declarations for both phone and iPad, all five settings previews, and system support for alternate icons. `assetutil` inspection confirms compiled icon images/stacks for every choice. The compiled default iPad icon was visually inspected at Home Screen size.
- Not verified: tapping the picker, actual Home Screen changes, selection persistence across relaunch, error presentation, older iOS versions, tinted/dark Home Screen appearances, and physical devices. Computer Use could not inspect Device Hub (repeated timeouts); an experimental runtime-switch test stalled and was stopped. It is not counted as a pass or retained as a hanging test. Full regression tests were not rerun for this change.
- Reproduce the packaging test with `xcodebuild -project Graphite.xcodeproj -scheme Graphite -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5)' -only-testing:GraphiteIntegrationTests/AppIconBundleTests test`.
- The target blue was RGB 48, 104, 232 (`#3068E8`). Generated foreground shading means this is a design color reference, not a claim of pixel-exact uniform RGB. Glass, ink, and pen are rendered in one foreground layer; arbitrary user-picked icon RGB is not implemented.

## Implemented and checked by hand in the iPad simulator

Checked on 2026-09-23 with a copy of a real course vault (653 files) on an iPad Pro 13-inch (M5) simulator:

- Opening a vault from On My iPad, restoring it on relaunch, folder tree, note names without `.md`.
- Settings: attachment location written to `.obsidian/app.json`; live "New files go to" preview.
- Drawing: full-screen canvas, canvas grows downward, SVG and PDF saved into `attachments/` with a shortest-path embed at the cursor, re-editing an SVG in place, drawings shown inline in the reading view.
- Saved SVG and PDF drawings rendered by macOS Quick Look (`qlmanage`), independently of Graphite.
- Write/Read toggle no longer inserts the last insertion a second time.

Checked on 2026-09-23 on the same simulator and vault copy (phase 3):

- Reading view, Live Preview and Source mode, switched from the toolbar's book button and the More menu.
- Live Preview: markup hidden away from the cursor's line; properties panel with typed rows; tables, math blocks, callouts (as boxes, with bold, italics and math inside), images, the inline video player, PDFs and embedded bases drawn in place, each with a `</>` button that shows the source; bullets, task checkboxes (tapping one toggles `[ ]`/`[x]` in the file), quote bars, `Note > Heading` link text, and `$…$` inline math drawn over the hidden source.
- Reading view: tables with thin borders, math in table cells (including `$k_n$` and `$x_n$`), colors and nested colors, highlights, callouts, task checkboxes in place of bullets, accent-colored links, the inline video player with a poster frame.
- Embedded notes: `![[4_Logic design#Saturation Region]]` renders that heading's section (callouts, math, images) inline in Live Preview under a link to it, one level deep.
- Table-of-contents links: `[[#Heading]]` scrolls to the heading in both views; Outline panel jumps put the heading at the top without raising the keyboard, and rendered images land in place after the jump.
- Search results are named by file; folders expand when tapped instead of opening.
- Bases: a `.base` file opens as a table (formula column, sort, display names) and as cards; tag lists stay readable in cells and cards; a `base` code block in a note renders as a live list in Live Preview.
- PDF view (checked by the PDF work on an iPad Pro 11-inch simulator, with Draw with Finger on): drawing, erasing, undo, zoom; highlight in two colors, underline, removing an underline; thumbnails with live ink; Go to Page; inserting a dotted page, adding and removing bookmarks, rotating a page and drawing on it; the delete confirmation and export dialogs; an embed starting at `#page=3` with its thumbnails and saved annotations; the conflict banner and Save a Copy when the file is replaced from outside. Saved files render correctly in `pdftoppm`, Ghostscript and `qlmanage`.
- PDF embeds in notes (checked here): the Obsidian-style toolbar (pages, zoom, "1 of 65", annotate, open) in Live Preview, and the tool picker appearing when annotating from the embed.
- Settings laid out like Obsidian's: Options (General, Editor, Files and links, Appearance, Core plugins) and plugin pages (Colors palette, Pencil drawings with the drawing format and background, Audio recorder).

Checked on 2026-09-23 on the same simulator (vault switching):

- The vault opened by the previous version carried over into the vault list.
- The vault switcher at the bottom of the sidebar lists the vaults with the open one checked; choosing another switches in place, and each vault reopens the note that was last open in it. Relaunching restores the most recent vault and its note.
- Create New Vault in Graphite's own folder: an empty "Physics" folder appears in Files under On My iPad › Graphite and opens as the vault.
- Open Folder as Vault through the Files picker; picking a folder that is already listed reuses its entry instead of adding a duplicate.
- Removing a vault from the list by swiping leaves its folder and files untouched.
- With no vault open, the welcome screen offers the list, Create New Vault and Open Folder as Vault. Settings › General shows the vault's location in the same words and leads to the vault manager.

Checked on 2026-09-23 on the same simulator with the edge-case vault:

- Appearance: the accent color (blue by default, nine presets, a custom color) applies live to the sidebar, toolbars, links, properties and rendered blocks; document toolbars use neutral icons. Rendered blocks, including embedded bases, follow a switch between light and dark while a note is open.
- Bases inside notes grow with their rows and scroll with the note; the Restaurants cards, gallery and map views render, as do Everything.base (table with sort markers and summaries, cards) and Cards.base (remote, local and solid-color covers).
- Display math: one-line and multi-line `$$` blocks, `aligned`, `align` with `\tag`, `cases`, matrices, fractions, roots, sums and integrals render centered. Search lists "06 Math" first for "06 Math".
- Opening a file inside a folder from the sidebar (broken earlier in this session by a change to folder rows, and fixed).
- The properties panel's Add field sits right after a list's items.

Checked on 2026-09-23 on the same simulator (properties, inline title, zoom):

- Properties laid out like Obsidian's, compared with a screenshot of Obsidian (`Edge cases/Earlier checks/2a. Processor, IOs, and Exceptions.md`): the note's name as a large inline title (Settings › Appearance › Show inline title), a "Properties" heading that folds them, grey names with type icons (a tag icon for `tags`), tags as accent chips and links as underlined accent links that wrap onto more lines, a checkbox, a date, and a grey "Add property".
- Re-editing drawings: a drawing in a note carries an Edit button, and tapping it (or the drawing) opens it in the drawing editor with its strokes, in Live Preview and in reading view. A stroke added there is saved into the same file and shows in the note. Other images open full screen to zoom. The two drawings made earlier in Graphite's own folder still read as editable.
- File names are shown with their extensions (`Math test.md`) in the sidebar, search, backlinks, the link picker and the title bar; Settings › Files and links › Show file extensions turns it off.
- Pinch zoom while annotating, in the PDF view and in a PDF embedded in a note, and in the drawing canvas. Image files open fitted and centered and zoom with a pinch; tapping an image in Live Preview opens it full screen to zoom, and Done closes it without changing the note.

Checked on 2026-09-23 on the same simulator (fixes from the Obsidian comparison, `Edge cases/Markdown/14 Review fixes.md` and `Edge cases/Search/`):

- Search: `zebrafish -lab` excludes, `line:(…) OR task-todo:…` combines operators; each result lists its matching lines with the words marked and a count. Typing `"` on the iPad keyboard gives curly quotes, which now quote a phrase too (found during this check).
- `#2026-exam` and `#y1984` are tinted as tags, `#1984` is not.
- `![Diagram|150](…)` is drawn 150 points wide; `![[Diagram.png|Caption|300x100]]` fits the box without stretching.
- The audio player returns to Play when a recording ends, and Play starts it again from the beginning.
- `[[Twelve pages with outline.pdf#page=3]]` opens the PDF at page 3; Open in PDF View from an embed scrolled to page 2 opens the PDF at page 2.
- Format › Heading › Heading 2 turns the cursor's whole line into a heading and back, and the note no longer jumps to its end after a Format command (found during this check).
- An embed written inside backticks in a callout stays text in Live Preview instead of turning into a file path (found during this check).

Checked on 2026-09-23 on the same simulator (file management, `Edge cases/File management/`):

- Long-press menus on files (Rename, Move to, Make a Copy, Share, Copy Vault Path, Delete) and folders (New Note, Notebook, Base and Folder inside it, Rename, Move to, Make a Copy, Copy Vault Path, Delete).
- Renaming "Rename me" asked "Update links?" with the right count (6 links in 1 note) and rewrote them all, including the property and the embed; the open note refreshed. "Always Update Links" was saved as `alwaysUpdateLinks` in `app.json`, and the next moves updated links without asking. Moving into a folder with Move to… (searchable folder list) and renaming that folder rewrote the Markdown and path links.
- Make a Copy created and opened "About file management 1.md"; Delete asked first and moved it to the storage's trash (Files › Recently Deleted).
- New Folder and New Note inside it from the folder's menu; the new-note sheet says which folder the note goes into.
- Renaming the open note from the title bar's menu (iPadOS's own rename field).
- The sidebar's ⋯ menu: sort by modified time (saved as `fileSortOrder`), Reveal Current File, Collapse All. Open folders are remembered per vault across relaunches.
- Found and fixed during this check: the rename field did not select the name; "Update Links" did nothing because the dialog cleared the pending move before its button ran; moving to the system trash deadlocked the vault's file actor (every later save would have hung); the title bar reported a rename twice and the second attempt showed an error; the new-note sheet did not focus its name field.

Checked on 2026-09-23 on the same simulator (getting around):

- The quick switcher (toolbar button, or ⌘O with a keyboard): "zebra" found "Zebra note" with the letters marked; opening it enabled Back, and Back and Forward moved between the two notes. Empty, it listed the recently opened file, also after a relaunch. A name that is not a note offered Create, which made the note in the vault's new-note folder.
- The command palette (toolbar button, or ⌘P): lists every command with its shortcut; "read" then Return ran "Toggle reading view".
- Found and fixed during this check: the rows' icons and folder names were nearly invisible on the translucent panel.

Checked on 2026-09-23 on the same simulator (typing, `Edge cases/Markdown/15 Typing.md`):

- Return after "- first item" continued the list twice; Return on the empty item ended it. `(` and `[[` were typed in pairs (`f(x…)`, `[[Zebra]]`).
- The toolbar above the keyboard: Indent (a tab), Task, Outdent, and Bold on the word at the cursor, pressed twice to remove it again; Find in note opened the system find bar with its match count.
- Found and fixed during this check: Return in the quick switcher right after typing created a new note instead of opening the existing one (the results had not arrived yet); the toolbar had no background, so the sidebar showed through it; a rendered block (the callout) disappeared whenever its first lines scrolled above the visible area, for example when the keyboard came up, an older Live Preview bug.
- Known issue: while the find bar shows matches inside a rendered block, that block is hidden until the find bar closes.

Checked on 2026-09-23 on the same simulator (link and tag suggestions):

- Typing `[[zeb` listed "Zebra note" first under the cursor; Return inserted `[[Zebra note]]` with the brackets closed once. `[[Lab notebook#` listed that note's headings; tapping "Result" gave `[[Lab notebook#Result]]`. `[[Lab notebook#^growth` listed the matching paragraph; choosing it inserted `[[Lab notebook#^7943cj]]` and wrote `^7943cj` at the end of that paragraph in the other note.
- `#2026` listed `#2026-exam` (2 notes) and `#2026year`; Return completed the tag.
- Tapping `[[A note that does not exist yet]]` created that note in the vault folder and opened it.
- Following `[[Lab notebook#^7943cj]]` opened that note scrolled to the paragraph and marked it briefly; the `^7943cj` marker is hidden away from the cursor and in reading view. `![[Note#^id]]` embeds show only that block.
- Found and fixed during this check: tag suggestions did not appear while the tag was still only digits; following a link that opened another note at a block or search match crashed, because the system's find highlight asserts when the note's view is not in the window yet (Graphite now draws its own mark).

Checked on 2026-09-23 on a second simulator of my own ("Graphite Features iPad", portrait), because another session was reinstalling the app and running tests on the first one (images into notes):

- The keyboard toolbar's Attach menu: Photo Library, Choose File (Take Photo appears only on devices with a camera). A photo picked from the library was saved as `Pasted image 20260923222649.jpg` (a real JPEG, 3000×2002) in the note's attachment folder and embedded at the cursor, and it rendered in the note.
- Copy Image from an image's menu, then Paste in another note: saved as `Pasted image 20260923232344.png` at the vault's root (this vault has no attachment setting, so Obsidian's default applies) and embedded.
- Found and fixed during this check: Live Preview crashed when an edit (Backspace next to a rendered block, or a paste) was applied while its blocks still described the previous text; an embedded image not yet indexed showed "not in this vault" until the note was reopened (embeds now look again when the index changes, in both views); Return in the quick switcher could create a duplicate note while the vault was still being indexed (it now needs a tap then); pasted images were read after the paste action ended, which iOS refuses (they are now read during it).

Checked on 2026-09-24 on the same simulator (tabs and split view, `Edge cases/Tabs/About tabs.md`):

- The tab bar: a restored note became the only tab; "+" opened a new tab showing recent files and ways to create or find a file; switching tabs kept each note's scroll position; closing a tab with its × and bringing it back with "Reopen closed tab" in the command palette; long press on a tab: Close, Close Other Tabs, Pin, Move to the Right, Reveal in Sidebar, Rename…; dragging a tab reordered the bar.
- Split Right opened an empty right side; Go to File… opened "Twelve pages with outline.pdf" there. The note's page links moved the PDF to pages 5, 9 and 12 while the note kept focus; the PDF's pen tools show only while its side is focused. Dragging the divider resized the sides and the PDF kept the page at the top in both directions; the position was saved.
- A pinned tab kept its note when a link was followed from it: the target opened in a new tab beside it.
- The sidebar's "Open to the Right" on "Tab target.md"; renaming it from the sidebar (with "Update links?") renamed its tab on the other side; deleting it closed that tab and the PDF's tab became active there.
- Closing the right side with its button; Move to the Right from a tab's menu recreated it; a note changed on disk by another app reloaded in its tab; quitting and relaunching restored both sides, the active tabs, the pins and the divider.
- A tab whose file could not be read (not UTF-8) showed why, with Try Again and Close Tab, instead of an alert; after the file was fixed, Try Again opened it.
- Typing in either note: the side touched takes focus, the toolbar and title follow it, and the cursor lands where tapped.
- Found and fixed during this check: closing the right side crashed (the split view read a side that had just closed); the PDF jumped several pages whenever its width changed (sidebar, divider), because autoscaling kept the scroll offset while changing the zoom; a rendered block vanished from a note when the other note took the keyboard; the recent-files list offered a file that had been deleted outside Graphite; the first tap into a note that was not being edited put the cursor at the end of the note instead of the tapped word, an older bug (revealing the old cursor line's markup invalidated the layout just before the tap was placed); a SwiftUI tap gesture used to focus a side competed with the editor's own taps and was replaced by an observer that only watches touches.

Checked on 2026-09-24 on the same simulator (PDF study tools, `Edge cases/PDF study/About PDF study tools.md`):

- Find in "Twelve pages with outline.pdf" from the PDF's toolbar: "boxing" gave 12 matches, marked on the page, and the arrows moved between pages.
- Long press on the PDF's text: the selection menu lists Copy Link to Page, Copy as Quote and Quote in "About PDF study tools.md" after the markup actions. Quote in added the paragraph as a quote, its three lines joined, followed by `[[Twelve pages with outline.pdf#page=2|Twelve pages with outline, p.2]]`, at the end of the note (the cursor had not been placed), with a blank line on each side.
- `[[Twelve pages with outline.pdf#page=7&selection=0,0,2,10|…]]` opened the PDF on the other side at page 7, and the note kept focus.
- "Locked with password graphite.pdf" (in the test vault; the password is "graphite") asked for its password in the tab; a wrong password said so; the right one opened it read-only, with a banner, no pen tools and no page actions, at the page its link asked for. PDFKit's own selection menu there has Copy Link to Page and Copy as Quote; the copied link was `[[Locked with password graphite.pdf#page=1|Locked with password graphite, p.1]]`. After a relaunch the password was asked again.
- Found and fixed during this check: the first quote went to the top of the note, above its heading, because a note never edited has its cursor at the start; a quote was followed by the next line directly, which a paragraph would have continued; unlocking a PDF opened it at page 1 instead of the page its link asked for.

Checked on 2026-09-24 on the same simulator (templates and daily notes, `Edge cases/Templates/About templates.md`; the test vault now has `.obsidian/templates.json`, `.obsidian/daily-notes.json`, a Templates folder and a Journal):

- Insert › Template… listed Checklist, Daily and Lecture; "Lecture" went in at the cursor with the note's name, "Thursday, September 24th 2026" and the time, and the note's tags became course and lecture, with a `course` property added, in one edit.
- The sidebar's New menu › Today's Daily Note created `Journal/2026/09/2026-09-24.md` from "Templates/Daily" (its heading "Thursday, 24 September 2026" and links to yesterday and tomorrow) and opened it. "Daily notes: Open previous daily note" went to 22 September, skipping the 23rd, which has no note.
- Settings › Daily notes showed the vault's settings and today's path; turning on "Open daily note on startup" wrote `"autorun": true` and kept the other keys, and at the next launch today's note opened. Settings › Templates showed the folder and formats, with examples.
- Found and fixed during this check: touches on a sheet, menu or the quick switcher focused the side of the split beneath them, which showed a PDF's pen tools over the Settings sheet (touches are now taken only from the window's own content, and not while a palette is open); `paths(inside:)` found nothing for the vault root, which would have hidden daily notes kept at the root; an insertion into a note in reading view waited for an editor that was not there (it now changes the text directly).
- Inserting "Checklist" and pressing the keyboard toolbar's undo returned the note to exactly its previous bytes.
- Note: the simulator tool's instant taps do not flip iOS 26 switches (a drag does); this is the tool, not Graphite.

Checked on 2026-09-24 on the same simulator (backlinks and unlinked mentions, `Edge cases/Mentions/About mentions.md`):

- "Zebra note" › right sidebar › Backlinks: Linked mentions listed "Linking the zebra" with the lines of its two links (one with a heading and alias), and "Naming the zebra" with its link. Unlinked mentions listed "Naming the zebra" with its two plain mentions (not the ones in code, in a web address, or in "ZebraNotes"), and "About search tests" and "About mentions" with theirs.
- Link beside "the zebra note" in "Naming the zebra" (not open) wrote `[[Zebra note|zebra note]]` to the file, and the line moved to Linked mentions. Link beside "The Zebra note" while "Naming the zebra" was open on the other side changed it through its editor to `[[Zebra note]]`, which autosaved.
- Tapping a linked line opened "Naming the zebra" at that line, marked.
- Found and fixed during this check: property values vanished in a narrow pane (a split beside the right sidebar), because the names always took 210 points; the right sidebar forgot its panel and the unlinked mentions list whenever the note changed; after a relaunch a side's tab bar did not scroll its active tab into view; excerpts were centered.

Checked on 2026-09-24 on the same simulator (folding, `Edge cases/Markdown/16 Folding.md`):

- Chevrons beside every heading and every list item with indented lines. Folding "Lecture 1" hid its paragraph, list, subheading, formula and table, and showed "…"; tapping "…" unfolded it and the rendered blocks came back. Folding "Topic A" hid its children.
- Typing at the end of a folded list item added to that line and kept it folded. Folds were remembered after a relaunch.
- A heading folded in reading view was folded in the editor, and the outline's jump to "Examples", inside that fold, unfolded it.
- Found and fixed during this check: after rendered blocks settled their heights, the text near the bottom of the view was not laid out, so a table and chevrons there appeared only after scrolling (an older Live Preview bug, now the viewport is laid out again); typing at the end of a folded line moved the caret to the end of the hidden section, because the fold check ran with the positions from before the edit; editing a folded line's text unfolded it and left its hidden lines collapsed.

Checked on 2026-09-24 on the same simulator (footnotes, `Edge cases/Markdown/17 Footnotes.md`):

- Live Preview: references drawn small and raised in the accent color, `[^fourier]` in code untouched, definition labels dimmed.
- Reading view: raised numbers 1, 2 and 3 in order of first reference, the second `[^fourier]` numbered 1 again, `[^missing]` (no definition) left as text, and the three footnotes listed after a rule at the end, the continuation line included.
- The right sidebar's Footnotes panel listed them with their emphasis; tapping footnote 2 showed its reference in the note.
- Found and fixed during this check: the panel showed footnotes as raw Markdown in the accent color.

Checked on 2026-09-24 on the same simulator (file recovery):

- Typing into `17 Footnotes.md` kept a copy of the saved text in Application Support on the first save. More › File Recovery… listed it; Restore This Version put the old text back through the editor (the word count dropped from 167 to 164), the file on disk followed, and the replaced version became a second copy. Undo in the editor brought the edit back.
- Deleting `Tab target.md` copied it first. "File recovery: Browse all snapshots" listed it as Deleted; restoring it recreated the file byte for byte in its folder and opened it. Settings › File recovery's View Snapshots opened the same list.
- Found and fixed during this check: a note just created was copied empty on its first save; the dialog for a deleted note said the current version would be kept; a copy dated in the future (after a clock change) would have stopped new copies until that time; removing a vault from the list left its copies on the device with nothing to show them (they are deleted now, and the list says so); a new note did not take the cursor, so typing went nowhere until the note was tapped.

Checked on 2026-09-24 on a new simulator, "Graphite Sidebar iPad", with a fresh copy of the test vault (Tags and Properties views):

- The sidebar's Files, Tags and Properties switcher; tags most used first with counts, nested tags under a chevron (book › fiction 9), and tapping fiction searched `tag:#book/fiction` with 9 results.
- Properties with their type icons and counts; Rename of `finished` (16 notes) from the row's menu changed only that line in each note, including Dune while it was open; renaming it back left every note identical to the test vault, byte for byte; Undo in Dune's editor reverted only Dune, and Redo restored it.
- Found and fixed during this check: the lists stayed empty, because a section's `.task` inside a `List` runs per row; the switcher let rows show through when scrolled; the Rename button was not disabled for an empty name (alert buttons ignore it; the workspace now says a name is needed); a note whose name has an accent (`L'Étranger.md`) was indexed twice, once as the scanner's composed name and once as the decomposed form a file URL gave after a save, so counts were one too high: vault paths are now always in Unicode's composed form, and a full scan removes the duplicates.

Checked on 2026-09-24 on "Graphite Sidebar iPad" (find in a note, after a crash report):

- A crash report from the Mac on 2026-09-23 showed UIKit raising while the find bar's highlights laid out for a note no longer on screen. Closing the find bar is not enough: the system's find session survives it and can still raise (the other session reproduced this in 4 of 4 cases in a Mac Catalyst harness). The editor now turns its find interaction off when it leaves the window and on again when it returns. "Search current file" and "Search & replace in current file" in the command palette open the find bar without a keyboard (from reading view too). Searching "Dune" in Dune.md found 4 matches; opening a new tab with the find bar up closed it cleanly, and the app kept running; back in the note, find still worked (13 matches for "bookmark"). The original crash was on the Mac and was not reproduced before the fix, so this shows the new path works, not that the old one crashed here.

Checked on 2026-09-24 on "Graphite Sidebar iPad" (bookmarks, `Edge cases/Bookmarks/About bookmarks.md` and the test vault's `.obsidian/bookmarks.json`):

- Bookmarks listed every kind from Obsidian's file: the note, "About bookmarks › Second section", the group Study (a search, the Library folder, "The key idea"), "Library graph" (opens in Obsidian), a web page, and "Deleted note" (not in this vault).
- The heading bookmark opened the note at Second section; "The key idea" at the `^key-idea` paragraph; the folder bookmark opened Files with Library expanded; the search bookmark showed its 9 notes.
- Renaming the note twice from its title moved its three bookmarks each time, headings and names kept. Remove, Rename, the More menu's Remove Bookmark and the palette's "Bookmark current file" changed the file as Obsidian would, keeping the graph's `options` and every `ctime`.
- Found and fixed during this check: the Tags and Properties lists said "No tags in this vault yet" while the first scan was still running; a change reported for `.obsidian/bookmarks.json` put it in the index as a file, so the file count grew by one; indexes were named after the app's folder, which iOS changes on reinstall and update, so each one rebuilt from scratch and the old ones stayed behind (four 9 MB copies on this simulator); the graph row's "Opens in Obsidian" sat under its icon; a missing note's menu offered to open it.

Checked on 2026-09-24 on "Graphite Sidebar iPad" (graph view, the test vault):

- The right sidebar's graph panel ("Graph view: Open local graph") showed Dune with Frank Herbert and Children of Dune, names beside each; depth 2 added "Untitled book without properties"; tapping Children of Dune opened it and the graph moved its highlight there.
- "Graph view: Open graph view" drew the whole vault, 476 items and 1'690 links, with the benchmark cluster, the fan-shaped notes, and unlinked notes in an outer ring; the open note is blue. Filtering by "Library" left 16 books with their names; tapping Frankenstein closed the graph and opened it. Pinching zoomed and showed names; Show All fitted the graph again, kept from the last time; dragging a hub moved it with its notes; turning Tags on added them in green (699 items).
- On this Mac, a synthetic vault of 10,000 notes builds its graph in 0.29 s and steps the layout in 8 ms; 100,000 notes take 3.3 s and 120 ms a step, so a graph that size takes about half a minute to settle.
- Found and fixed during this check: building the graph ran several queries per link and took 27 s for 10,000 notes (now resolved in memory, checked against the index); the view fitted the graph again while a node was dragged, moving it under the finger; names at the edges were cut off; a filtered graph kept the zoom of the previous one; the settings panel cut off the Forces; "1 links".

Checked on 2026-09-24 on "Graphite Sidebar iPad" (deep links, with `xcrun simctl openurl`):

- With Graphite closed, `graphite://open?vault=Scratch&file=Bookmarks%20guide%23Second%20section` asked "Open in Graphite?", then started it and showed the note at Second section.
- `new` with `file=Inbox/From a link` created and opened the note; the same with `append&silent` added a line through the open editor; without `append` it made "From a link 2.md", not opened because of `silent`.
- A link naming a vault not in the list said so; `search` with `tag:#book/fiction` showed its 9 notes.
- Found and fixed during this check: a search asked for while the left sidebar was hidden (the link, ⇧⌘F, the palette's "Search in all files", a tag in the graph) set the query without showing it.

Checked on 2026-09-24 (recordings that survive a crash):

- Which formats a crash leaves playable was measured on this Mac by copying each file while it was still being written, five seconds in: M4A (what the recorder wrote) and AAC in CAF, nothing; PCM in CAF, 4.94 s at 317 MB an hour; IMA4 in CAF, 4.92 s at 86 MB an hour. Recordings are now IMA4 in CAF until Stop converts them to M4A.
- In the simulator, the Mac has not allowed the Simulator app to use its microphone, so no audio arrives: each audio call there waits 30 seconds and fails. This showed that starting a recording blocked the main thread (the whole app froze for 90 seconds); starting and resuming now happen off it, and the app stayed usable while the start waited, showing "Starting…" with a Cancel in its menu (which worked). A start that fails or is cancelled removes its empty file. Recording itself, and recovery after the app is killed while recording, still need an iPad or a simulator with microphone access.
- Found and fixed during this check: ⌃⌘S was both iPadOS's Show Sidebar and Graphite's Show or Hide Files, which the menu bar reported as a conflict; the recording control's row of buttons was cut to one in the toolbar, so Pause and Stop could not be reached (it is now one menu with the time and every action).

Checked on 2026-09-25 on "Graphite Sidebar iPad", after the bug-fix merge:

- Yesterday's features still work after the merge: a `graphite://` link opened "Bookmarks guide" at Second section, and the Bookmarks panel, graph and recordings code were all still in place.
- Found and fixed: after a jump down a note (a link or bookmark to a heading), opening the left sidebar left a rendered callout blank, with its space kept, and the fold chevrons beside the wrong lines, until the next scroll. The pre-merge build did the same, so it was older than the merge. When the text column's width changes (the sidebar, a split, a rotation), the editor now waits for the width to settle, lays the visible text out again and places the rendered blocks and chevrons on it. It happened in both tries before the fix and neither after it.
- iCloud Drive, which syncs the Desktop this repository is on, kept 69 older copies of source files as "Name 2.swift" when the merge replaced them, and the build failed on the duplicate declarations. They were moved out of the repository (to `~/.graphite-fix-work/icloud-conflict-copies-2026-09-25/`), and the build and tests pass again.
- Found and fixed (reported by the user): in the file list, the contents of an expanded folder were drawn centered and squeezed together instead of as rows. The merge had wrapped each row in `.equatable()`, which makes a folder's row a single view, so the list drew the folder's contents inside it. Without the wrapper (the rows still compare by their entry), files and folders three levels deep line up with their folders, and tapping one opens it and highlights its whole row.
- The iPad's own crash logs (six copied of the seven there, 23–24 Sep, all on the build installed on 23 Sep) are one crash: indexing ran out of stack parsing a note with lists nested more than about 55 deep, and because indexing runs at every launch the app crashed again on each reopen. The current code no longer hands such a note to the Markdown parser (`MarkdownNesting`, tested on a thread with the same 512 KB stack); a Release build with that change was installed on the iPad the same evening, and 45 seconds after launch it was still running with no new crash log. Whether the note that caused the crash is in the vault the iPad opens is not known, so this does not show that the crash itself is gone.

## Implemented but not yet verified

- Recording a lecture and recovering it after Graphite is closed mid-recording, with a real microphone (see the check above).

- The camera (the simulator has none).
- Pasting a web page as Markdown in the app (the conversion is tested; the simulator's pasteboard sync delivers only promises).
- Dropping images, files, and sidebar items into a note (synthetic touches do not start drags).
- Arrow keys and Escape in the suggestion list with a hardware keyboard.
- Suggestions with Markdown links chosen instead of Wikilinks (Settings › Files and links).
- Tab and Shift-Tab, ⌘B, ⌘I, ⌘K, ⌘L, ⌥⌘↑ and ⌥⌘↓, and ⌃⌘1–6 in the editor with a hardware keyboard.
- Keyboard shortcuts with a hardware keyboard and the iPad menu bar: the File, View and Go menus (`GraphiteCommands`) with ⌘N, ⌘T, ⌘O, ⌘W, ⇧⌘T, ⌘S, ⌘,, ⇧⌘F, ⌘E, ⌘P, ⌃⌘S, ⌘[ and ⌘], ⌃Tab and ⌘1–9, and their enabled states; in the editor ⌘/, ⌥⌘F, and ⌥Return, ⌘Return and ⌥⌘Return on a link. The simulator tool types text but cannot hold ⌘, and its synthetic swipe does not open the menu bar. Arrow keys in the palettes.
- Dragging a file or folder onto a folder in the sidebar to move it: the simulator's synthetic touches open the menu instead of starting a drag, so it needs a real device or a pointer.
- Folding: arrow keys and Return at a folded line with a hardware keyboard (Return is covered by tests); "Fold All" and "Unfold All" from the More menu and the command palette; notes over 2 MB, which do not fold.
- Templates: the Templates and Daily notes plugins switched off in Core plugins; a daily note format with week numbers or times, which Graphite cannot read back from names, so previous and next skip those notes.
- Tabs: dragging a tab to the other side and dropping a sidebar file on a tab bar (tab reordering by drag was checked); ⌘T, ⌘W, ⇧⌘T, ⌃Tab, ⌃⇧Tab, ⌘1–⌘9, ⌘-tap and ⌥⌘-tap on links, and ⌘Return and ⌥⌘Return in the quick switcher, with a hardware keyboard; the split in a narrow window (Slide Over, a narrow Stage Manager window), where only the focused side shows with a button for the other; Apple Pencil drawing on a PDF focusing its side.

- Bases interactions by touch: header sorting, the view editor and property editor sheets, map markers and their popups, editing a property from a cell, the map view in general on the course vault. The Bases agent verified rendering of every view type and the file writes, but not taps.
- Turning each core plugin off and back on in a running session (the Settings pages and toggles were checked by eye, not each feature's disappearance).
- Properties editing through the panel (the file format is covered by tests).
- Dark and light appearance of drawn replacements (bullets, checkboxes, math) after switching appearance while a note is open.
- The macOS editor on a real Mac. It has no rendered blocks or drawn replacements, so list bullets, task boxes, quote markers, the `#` of `[[Note#Heading]]` and inline math stay visible there as styled source (`UiStylingFixTests`, `UiEditorMacEditorTests`). Heading and search jumps, link clicks (⌘ and ⌥⌘), pasted and dropped attachments (several items keep their order), and undo for text changed elsewhere are covered by `UiEditorMacEditorTests`; dragging from Finder or the sidebar has not been tried.
- Apple Pencil on PDFs. UIKit cannot tell Pencil from finger when choosing which view receives a touch, so while annotating every touch on a page goes to its canvas, which draws only with Pencil unless Draw with Finger is on.
- PDF page duplicate, move, drag reordering, multiple selection, page import, recoloring markup, PDFs with an existing table of contents, the dialog that warns about dropped document settings, embed zoom, Open in PDF View, and releasing an embed after scrolling away.
- Export a Copy (share sheet) from the drawing editor.
- Double-tap to zoom images (the simulator tool cannot send a real double tap), and editing property values by typing (changes are saved when the field loses focus).
- PNG drawing insertion through the UI (the file format is covered by tests).
- Transparent drawing backgrounds in other applications.
- Attachment import through the Insert menu with the new placement rules.
- Recording after the audio-session fix (needs a microphone). Also compiled for iOS and not device-tested: a recording that could not be saved blocks a new recording until it is saved with Try Saving Again (which can target a newly computed destination) or discarded; finalization publishes anyway after 30 s if the recorder never reports finishing; an iOS media-services reset or an encoding error stops the recorder and keeps the audio for retry; interruption, route-change and media-reset handling hops to the main actor. The controller's state rules are unit-tested on macOS (`AppleRecordingAccessRecordingTests`).
- Drawing drafts after the system ends the app, the drawing editor's own PencilKit undo history, and the PDF and SVG buttons of the alert for an oversized PNG: unit-tested only up to the draft store (`UiRootSettingsTests`); they need a device check.
- Other view and UIKit code changed by the 2026-09-25 bug-fix merge that no test exercises, verified only by building for iOS (and for macOS where it is not UIKit-only): in Live Preview, the pool of off-screen rendered blocks and its memory-warning release, the find interaction turned off when the editor leaves the window, jumps waiting for the window, inline math drawn once per formula and its image cache, quote bars on every line of a wrapped quote, pasted HTML converted and pasted images encoded off the main thread, suggestions not updated while an edit is being applied, the block checkpoints the styler resumes from, and `tabSize` in the iPad editor's Return handling; in PDFs, the eraser mask clipping the ink's appearance stream (`PDFPencilInk`), a second eraser cut converted again, the canvases deferring their ink re-editing records (the session writing them before saves is tested on macOS), stored ink decoded in the background, the save when the app goes to the background, scrolling held while an embed annotates, the markup tap, and the lock notice in protected embeds; drawings inserted after the selection, kept when their note is in conflict, and placed where the note's text moved; drops onto file rows (moving the items into that file's folder) and the sidebar's drop outline; the unplayable-media notice and the players' time updates in reading view.
- Search operators `task:` and `[property:null]`, and `tag:` finding nested tags: implemented, with no automated test yet.
- Obsidian's `smartIndentList`, `autoPairMarkdown`, `useTab` and `autoConvertHtml` read from `.obsidian/app.json`: implemented; no test reads or writes those keys (`autoPairBrackets` and `tabSize` are covered above).
- Everything that needs a physical iPad: Apple Pencil input and latency, `PKToolPicker` behavior with a Pencil, memory of large PNG exports, iCloud and third-party file providers (including safe writes staged in the volume's item-replacement folder).
- Vaults on a device: bookmarks surviving an app update, vaults in iCloud Drive (including Obsidian's folder), and Create New Vault inside a folder chosen with Choose Folder… (the simulator run used Graphite's own folder). On the Mac, creating a vault needs a chosen folder and was not tried.

## Not implemented yet

A full comparison with Obsidian mobile, ranked for studying on an iPad, is in `Docs/Obsidian-mobile-gaps.md`. The defects it found in existing features are fixed (see above). The main remaining items:

- Live Preview reveals markup a whole line at a time around the cursor, not one element at a time as Obsidian does.
- Toggling tasks from reading view (Live Preview toggles them).
- Resizable table columns in Bases (`columnSize`), custom map tiles (`mapTiles`), Kanban views, and `html()`.
- Choosing a different format for an existing drawing (use Export a Copy).
- Native text selection handles, magnifier, Look Up and Translate on PDFs while annotating: a long press selects a word and dragging extends it, Graphite's own implementation. Outside annotation, PDFKit's native selection is used.
- Undo for PDF page operations; undo history for pages scrolled far away is dropped.
- Embeds of one PDF now coordinate: before an embed annotates, the other embeds and a PDF view showing the file are saved. The PDF view does not do the same before its own edits, so editing a PDF in its view while an embed of it has unsaved annotations can still end in a conflict.
- Filling PDF forms: form fields are shown read-only.
- Lecture video recording; recording is audio only.
- Conflict versions kept by iCloud or another file provider (`NSFileVersion`) are neither detected nor shown. Graphite's own revision checks still refuse to overwrite a file changed elsewhere.
- Hidden tabs keep their note sessions open; only hidden PDFs beyond the most recently visible one are released.
- Rendering SVG files made by other applications inside the reading view (they open with the system preview instead).
- Tabs: undo history does not survive switching tabs or moving a tab to the other side (the editor is rebuilt; the note's text, cursor and scroll position are kept); reading view does not keep its scroll position across tab switches; a file is open in at most one tab, so "Open in New Tab" on a file already open shows its tab; tab history is not saved between launches; tabs stack at most two sides, left and right; no workspaces.
- Opening a second vault in its own window, as Obsidian does on the desktop; one vault is open at a time.
- Bases issues found with the edge-case vault and not fixed yet: a date Range across a daylight-saving change reads one hour short; anchors inside an entry Graphite rewrites are written out as copies, and custom tags there are dropped; an embed naming a missing view shows the first view without saying so; an invalid filter group is dropped with a warning, so the view lists every row; `inFolder` ignores case; `date(1980)` is read as milliseconds; `duration("")` is an error rather than empty.
- Equation numbers from `\tag` and numbered environments are not drawn (the equations are).
- Renaming or deleting a vault's folder from Graphite (do it in Files, then open the folder again).
- Per-vault Graphite preferences: which core plugins are on, the editor defaults and the Colors palette apply to every vault on the device, where Obsidian keeps core plugin choices per vault.
- 10,000 and 100,000 file measurements on hardware.
